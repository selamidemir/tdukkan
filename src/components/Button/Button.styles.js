import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 8,
    margin: 10,
    borderRadius: 5,
    backgroundColor: '#2286c3',
    alignItems: 'center',
  },
  title: {
    fontWeight: 'bold',
    fontSize: 17,
    color: 'white',
  }
});
